<?xml version="1.0" encoding="UTF-8" standalone="no"?>

<view xmlns="http://jmix.io/schema/flowui/view"
      title="${messageKeys['title']}">
    <facets>
        <dataLoadCoordinator auto="true"/>
    </facets>
    <layout><%if (bpmTaskName != null && bpmTaskName.trim() != '') {%>
        <h1 text="$bpmTaskName"/><%}%><%if (bpmTaskDueDate != null && bpmTaskDueDate.trim() != '') {%>
        <h4 text="Due date: $bpmTaskDueDate"/><%}%><%if (bpmTaskDetails != null && bpmTaskDetails.trim() != '') {%>
        <details summaryText="Task details">
            <p text="$bpmTaskDetails"/>
        </details><%}%>
        <formLayout>
        </formLayout>
        <hbox id="actionsPanel">
        </hbox>
    </layout>
</view>